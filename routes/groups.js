const express = require('express');
const router = express.Router();

// Placeholder route for groups
router.get('/', (req, res) => {
  res.json({ message: 'Groups endpoint not implemented yet' });
});

module.exports = router;
